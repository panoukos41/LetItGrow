@using LetItGrow.UI.Web.Components.Nodes.Forms

<Modal @ref="modal" LightDismiss="false">
    <CreateMeNodeForm @ref="form" OnCancelClick="modal.Toggle" />
</Modal>

@code{
    Modal modal = null!;

    CreateMeNodeForm form = null!;

    public void Show()
    {
        form.SetData(new());
        modal.Toggle();
        StateHasChanged();
    }
}