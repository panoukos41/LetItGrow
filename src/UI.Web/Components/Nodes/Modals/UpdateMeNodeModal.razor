@using LetItGrow.UI.Web.Components.Nodes.Forms
@using LetItGrow.Microservice.Data.Nodes.Models

@inject INodeService nodeService

<Modal @ref="modal" LightDismiss="false">
    <UpdateMeNodeForm @ref="form" OnCancelClick="modal.Toggle" />
</Modal>

@code{
    Modal modal = null!;

    UpdateMeNodeForm form = null!;

    public void Show(MeasurementNodeModel node)
    {
        form.SetNode(node);
        modal.Toggle();
        StateHasChanged();
    }
}