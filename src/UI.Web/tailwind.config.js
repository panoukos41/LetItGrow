module.exports = {
    purge: [
        './**/*.html',
        '.**/*.razor'
    ],
    darkMode: false, // or 'media' or 'class'
    plugins: [],
    variants: {
        extend: {
            //outline: ['hover'],
            //borderWidth: ['hover'],
        }
    }
}